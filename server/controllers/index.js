const {
    signUpController, 
    signInController, 
    getUserController, 
    logOutController
} = require('./users');

module.exports = {
    signUpController,
    signInController,
    getUserController,
    logOutController
}